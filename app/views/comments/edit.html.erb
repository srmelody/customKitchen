<h1>Editing comment</h1>

<%= error_messages_for :comment %>

<%= render :partial => @comment, 
  :locals => { :button_name => "Update"} %>

<%= link_to 'Show', [@recipe, @comment] %> |
<%= link_to 'Back', recipe_comments_path(@recipe) %>
